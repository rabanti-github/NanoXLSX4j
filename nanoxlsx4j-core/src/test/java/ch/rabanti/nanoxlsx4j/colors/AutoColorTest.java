package ch.rabanti.nanoxlsx4j.colors;

import static org.junit.jupiter.api.Assertions.assertNull;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class AutoColorTest {

    @Test
    @DisplayName("Test of the getter of the StringValue property (dummy / for code completion)")
    void stringValueTest() {
        AutoColor color = new AutoColor();
        assertNull(color.getStringValue());
    }
}
